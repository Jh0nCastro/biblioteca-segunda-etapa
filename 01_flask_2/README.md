# Entrega 1 — Atividades em sala: Flask 2

API Flask com CRUD de livros persistido em JSON. A função `acessar_livros`
consolida as operações de leitura e escrita, conforme solicitado.

Execute `python app.py` e acesse `GET /livros`.
